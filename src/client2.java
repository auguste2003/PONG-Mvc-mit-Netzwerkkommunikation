import Pong.Controller2.PongController;
import Pong.Model2.PongModel;
import Pong.View2.PongView;
import processing.core.PApplet;
import java.net.ServerSocket;
import java.net.Socket;
public class client2 {
    public static void main(String[] args){
        final int port =8080;
/*
        PongModel model = new PongModel(900, 600);
        var controller = PongController.newClient("localhost",port,600,model);
        var view = new PongView();



        // Connect M, V and C
        controller.setModel(model);
        controller.setView(view);
        view.setController(controller);

        // Starts the processing application
        PApplet.runSketch(new String[]{"PongView"}, view);

 */
    }


}